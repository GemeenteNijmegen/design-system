import readme from "@gemeentenijmegen/semantic-html/README.md?raw"
import { Meta, Source } from '@storybook/addon-docs';
import example from './example.html?raw';
import combined from './combined-example.html?raw';

<Meta title="foundations/Semantic HTML/Example"/>

## Example implementation only semantic HTML
<Source language="html" code={example} />

## Example implementation combining components and semantic HTML
This can be used if a part of the content on a page is comming from a WYSIWYG editor
<Source language="html" code={combined} />
